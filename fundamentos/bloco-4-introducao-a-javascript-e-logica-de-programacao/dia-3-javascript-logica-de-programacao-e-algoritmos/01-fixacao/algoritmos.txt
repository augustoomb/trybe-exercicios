Nesse primeiro exercício, percorra o array imprimindo todos os valores nele
 contidos com a função console.log() ;

 -Adicionar o array
 -Adicione um laço de repetição for-of
 -Use o console.log dentro do laço, mostrando o valor da posição atual



Para o segundo exercício, some todos os valores contidos no array e imprima o resultado;

-Adicionar o array
-Adicione uma variável com valor inicial 0
-Adicione um laço de repetição for-of
-A cada iteração, a variável criada deve receber seu próprio valor mais o valor contido
    na posição
-Ao final do laço, mostrar o conteudo com console.log


Para o terceiro exercício, calcule e imprima a média aritmética dos valores contidos no array;

-Adicionar o array
-Adicionar uma variável com valor inicial 0 (soma de todos os valores)
-Adicionar uma variável com valor inicial 0 (média dos valores)
-Adicione um laço de repetição for-of
-A cada iteração, a variável soma receberá ela mesmo mais o valor contido em cada posição
-No fim da iteração, a variável de média receberá a divisão da soma pelo tamanho do vetor



Com o mesmo código do exercício anterior, caso o valor final seja maior que 20, 
imprima a mensagem: "valor maior que 20". Caso não seja, imprima a mensagem: 
"valor menor ou igual a 20";

-Adicionar o array
-Adicionar uma variável com valor inicial 0 (soma de todos os valores)
-Adicionar uma variável com valor inicial 0 (média dos valores)
-Adicione um laço de repetição for-of
-A cada iteração, a variável soma receberá ela mesmo mais o valor contido em cada posição
-No fim da iteração, a variável de média receberá a divisão da soma pelo tamanho do vetor
-Adicionar um if caso a média seja maior que 20.
    -Dentro do if, mostrar a mensagem "Valor maior que 20"
-Caso não seja, adicione um else
    -No else, adicionar a mensagem "Valor menor ou igual a 20"




Utilizando for , descubra qual o maior valor contido no array e imprima-o;

-Adicionar o array
-Declarar uma variável com início 0
-Criar um laço for do tamanho do array
-Criar uma condição for dentro do laço comparando cada valor com a variável criada
    -Caso o valor do item seja maior, seu valor será atribuido a variável
-Fora do laço, imprimir a variável.
